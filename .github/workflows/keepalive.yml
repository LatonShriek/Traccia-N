name: Keep Supabase Active
on:
  schedule:
    - cron: '0 6 * * 1,4'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" "https://qfzpjebloxstjotdytbe.supabase.co/rest/v1/pazienti?select=id&limit=1"
